import Vue from 'vue'
import App from '../app.vue'

  // document.addEventListener('DOMContentLoaded', () => {
  //   const app = new Vue({
  //     el: '#hello',
  //     data: {
  //       message: "Can you say hello?"
  //     },
  //     components: { App }
  //   })
  // })

/*document.addEventListener('turbolinks:load', () => {
  if(document.getElementById('id_del_DOM')) {
    var app = new Vue({
      el: '#id_del_DOM',
      data: {
        show: false
      }
    })
  }
})*/
